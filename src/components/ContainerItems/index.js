import React from 'react';
import { ContainerItems as ContainerItem } from './styels';

function ContainerItems ({children}) {
    return <ContainerItem> {children} </ContainerItem>
}

export default ContainerItems;
